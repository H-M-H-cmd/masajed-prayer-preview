# Preview project runs entirely on mock data — no backend needed.
# NEXT_PUBLIC_APP_ENV must stay "development" so services return mocks.
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_APP_ENV=development
