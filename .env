# Committed defaults — this preview runs entirely on mock data, no real backend.
# NEXT_PUBLIC_APP_ENV must stay "development" so the service layer returns mocks.
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_APP_ENV=development
